server:
  port: 8080

spring:
  application:
    name: wejoinlife-api

  threads:
    virtual:
      enabled: true

  datasource:
    url: jdbc:mysql://:/?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: 
    password: 

  sql:
    init:
      mode: never # Disabled: will use existing database

application:
  security:
    jwt:
      secret-key: 
      expiration: 86400000
